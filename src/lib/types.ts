export type ClientInfo = {
    browser?: string,
    platform?: string,
    device?: string
    os?: string,
}

export type ClientIpandLocation = {
    ip: string,
    network: string,
    city: string,
    country_name: string,
    latitude: string,
    longitude: string,
    org: string,
    location: string
}